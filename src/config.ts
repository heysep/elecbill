/**
 * granite appName 과 동일. 슬롯 식별자라 앱 주제가 바뀌어도 바꾸지 않는다.
 * localStorage 키 프리픽스도 여기서 끌어다 쓴다 — 사용자에게 보이지 않는 값이다.
 */
export const APP_NAME = 'elecbill';
/** localStorage 키 프리픽스 */
export const STORAGE_PREFIX = 'elecbill.';
